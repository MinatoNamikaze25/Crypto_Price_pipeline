# Crypto_Price_pipeline
End-to-end crypto price analytics pipeline built on Databricks  using Medallion Architecture (Bronze → Silver → Gold), processing  CoinGecko API data with PySpark &amp; Delta Lake, orchestrated via  7-task Databricks Jobs pipeline with 13 gold tables and interactive dashboard.
